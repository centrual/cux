# GitHub uses this file to render the "Sponsor" button on the repo
# page. We point at the inulute landing page rather than a single
# payment provider so the project can grow channels without rotating
# the URL embedded in users' browsers, READMEs and stars.
custom: ["https://support.inulute.com"]
